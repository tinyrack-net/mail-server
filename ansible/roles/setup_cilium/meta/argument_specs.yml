---
argument_specs:
  main:
    short_description: Bootstrap and verify Cilium for K3s
    options:
      setup_cilium_version:
        type: str
        required: true
      setup_cilium_kube_context:
        type: str
        required: true
      setup_cilium_node_name:
        type: str
        required: true
      setup_cilium_device:
        type: str
        required: true
      setup_cilium_values_path:
        type: path
        required: true
