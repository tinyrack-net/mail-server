---
argument_specs:
  main:
    short_description: Restore and verify all Sealed Secrets controller recovery keys
    options:
      restore_sealed_secrets_key_namespace:
        type: str
        required: true
      restore_sealed_secrets_key_ready_timeout:
        type: str
        required: true
      restore_sealed_secrets_key_public_cert_path:
        type: str
        required: true
      restore_sealed_secrets_key_resources:
        type: list
        elements: dict
        required: true
