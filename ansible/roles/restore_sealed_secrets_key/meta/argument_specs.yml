---
argument_specs:
  main:
    short_description: Restore the Sealed Secrets controller recovery key without overwriting a different key
    options:
      restore_sealed_secrets_key_namespace:
        type: str
        required: true
      restore_sealed_secrets_key_secret_name:
        type: str
        required: true
      restore_sealed_secrets_key_ready_timeout:
        type: str
        required: true
      restore_sealed_secrets_key_tls_crt_path:
        type: str
        required: true
      restore_sealed_secrets_key_tls_key:
        type: str
        required: true
