---
argument_specs:
  main:
    short_description: Reconcile Stalwart ACME certificates and TLS listeners
    options:
      configure_stalwart_api_url:
        type: str
        required: true
      configure_stalwart_api_key:
        type: str
        required: true
      configure_stalwart_registry_account_id:
        type: str
        required: true
