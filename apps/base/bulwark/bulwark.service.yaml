apiVersion: v1
kind: Service
metadata:
  name: bulwark-service
  namespace: bulwark-system
spec:
  type: ClusterIP
  selector:
    app: bulwark
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http
